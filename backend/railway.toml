[deploy]
startCommand = "gunicorn server:app -k uvicorn.workers.UvicornWorker --workers 4 --worker-connections 1000 --backlog 2048 --timeout 120 --keep-alive 5 --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[deploy.healthcheck]
path = "/api/health"
intervalSeconds = 30
timeoutSeconds = 10
